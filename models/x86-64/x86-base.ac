AC_ISA(x86)
{
	ac_format FORMAT_DUMMY = "%dummy:32";
	
	ac_instr<FORMAT_DUMMY> mov, xor, pop;
	
	ac_behaviour mov, xor, pop;

	ISA_CTOR(x86) {
		mov.set_decoder();
		mov.set_behaviour(mov);
		
		pop.set_decoder();
		pop.set_behaviour(pop);
		
		xor.set_decoder();
		xor.set_behaviour(xor);
	};
	
};
